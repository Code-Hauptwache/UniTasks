#include "my_math_functions.h"
int absWertInt(int v)
{
    if(v<0)
        return -v;
    else
        return v;
}

 double absWertDouble (double v)
{
    if(v<0)
        return -v;
     else
         return v;
}
